interface:
  display_name: "Cowork"
  short_description: "Codex 规划审核、可选多 agent 执行的跨平台协作循环"
  default_prompt: "Use $cowork; first ask which execution agents to use, then run the approved multi-agent planner/reviewer loop."
